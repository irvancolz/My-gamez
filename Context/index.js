import SideNavContext from './sidenav'
import GameIdContext from "./gameId";

export default function Context(){
    return(
        {
            SideNavContext,
            GameIdContext,
        }
    )
}